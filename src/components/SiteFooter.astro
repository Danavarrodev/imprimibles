---
const year = new Date().getFullYear();
const links = [
  { href: "/sobre-mi", label: "Sobre mí" },
  { href: "/imprimibles", label: "Imprimibles" },
  { href: "/contacto", label: "Contacto" },
];
---

<footer class="mt-16 border-t">
  <div class="mx-auto max-w-7xl px-4 py-8">
    <div class="flex flex-col gap-6 md:flex-row md:items-center md:justify-between">
      <div>
        <a href="/" class="text-lg font-semibold">imprimibles</a>
        <p class="mt-1 text-sm text-slate-600">Fichas listas para descargar en PDF.</p>
      </div>

      <nav class="flex flex-wrap gap-4 text-sm text-slate-700">
        {links.map(l => <a href={l.href} class="hover:underline">{l.label}</a>)}
      </nav>

      <div class="text-sm text-slate-500">
        © {year} · ImprimiblesEdc
      </div>
    </div>
  </div>
</footer>
